import React from "react";
import ReactDOM from "react-dom/client";
import "./selfindex.css";
import App from "./selfApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
